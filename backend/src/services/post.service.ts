export class PostService {
    async createPost(data: { content: string, authorId: number }) {
        // Logica per creare il post nel database
    }

    async getPosts() {
        // Logica per ottenere i post dal database
    }
}
